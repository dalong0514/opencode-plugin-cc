{
  "name": "opencode-plugin-cc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Use opencode models from Claude Code to delegate tasks or review code.",
  "license": "MIT",
  "engines": { "node": ">=18.18.0" },
  "scripts": {
    "test": "node --test tests/*.test.mjs"
  }
}
